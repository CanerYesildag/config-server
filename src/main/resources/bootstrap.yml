spring:
  application:
    name: config-server
---
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/CanerYesildag/config-repository.git
          force-pull: false
          search-paths: /**